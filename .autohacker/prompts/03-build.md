# Phase 3 — Build and validate (Loop {{LOOP_INDEX}})

Read {{BRAIN_FILE}}, {{EVIDENCE_DIGEST}}, {{INVEST_LOG}}, {{PLAN_LOG}}, {{EVIDENCE_BUNDLE}}.

Implement the full master plan:
1. UX fixes for void gaps, hidden-value clicks, whitespace, duplicate copy, scope dead space, inline direct-value.
2. **4+ edge cases per major item** aligned to {{CORE_VALUES}}.
3. Create/update `tests/Delivera-Governance-AutoHacker-v6-Unified-Realtime-Validation-Tests.spec.js` — fail-fast (`--max-failures=1`), realtime UI queries after each step, console capture, journey value not legacy noise.
4. Run `npm run build:css` then the npm command in {{TEST_TARGET_FILE}} before done.
5. Write implementation notes to **{{BUILD_LOG}}**.
6. **4+ bonus creativity** items within scope.

Do NOT modify `.autohacker/AutoHacker.ps1`.

{{PATCH_TEXT}}
